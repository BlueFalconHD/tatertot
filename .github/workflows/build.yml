on: [push]

jobs:
  build:
    name: "🥔 Build Static Tatertot"
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 16
    - run: npm ci
    - run: node ./src/index.js
